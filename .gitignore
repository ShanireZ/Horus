# === .NET (agent/) ===
[Bb]in/
[Oo]bj/
# 成品发布物(真机验收包·体积大·含运行时/原生库)——不入 git,只在磁盘 dist/。
dist/
.vs/
*.user
*.suo
*.nupkg
*.binlog
[Tt]est[Rr]esults/

# === 编辑器 ===
.vscode/
.idea/
*.swp

# === Horus 运行时 / 机密(切勿提交) ===
# 真实配置含 PSK / 管理令牌 / 视觉 API key —— 只提交 *.sample.json,真值放 *.config.json
agent.config.json
**/agent.config.json
server.config.json
**/server.config.json
# Agent 断网缓冲:运行时目录紧贴 exe(agent/Program.cs 用 AppContext.BaseDirectory/buffer),
# 即只会落在 agent/bin 或发布物 dist 之下;仅列运行时子目录,
# 避免笼统 **/buffer/ 在 Windows git 大小写不敏感下误吞源码目录 agentcore/Buffer/。
agent/bin/**/buffer/
dist/**/buffer/
# 服务器运行时:SQLite 库与证据截图
*.db
*.db-wal
*.db-shm
*.sqlite
/images/
/archive/
# 开发期从 server/ 起服务时的运行时数据目录(dataDir=./data → server/data;仅列运行时子目录,
# 避免笼统 server/data/ 在 Windows git 大小写不敏感下误吞源码目录 server/Data/)
server/data/images/
server/data/archive/
# CLIP ONNX 模型(~335MB·超 GitHub 单文件 100MB 上限)——部署时人工放置,不入 git
*.onnx

# === 证书 / 环境机密(切勿提交) ===
.env
.env.*
!.env.example
*.pfx
*.snk
*.key
*.pem
*.log

# === 依赖(未来服务器端) ===
node_modules/

# === OS ===
Thumbs.db
.DS_Store
desktop.ini

# 注意:agents.md 为项目准则,必须跟踪,勿在此排除。

# === 主页（web/）的品牌资产 ===
# ★★★ 这一份是**装配出来的副本，不入库**。权威在 assets/brand/，
#   而那份本身已经是 BetaPass/std/brand/horus/ 的逐字节镜像（见其 PROVENANCE.md）——
#   再入库一份就是**第三份抄件**：上游换一次 logo，三处一起过期而彼此自洽，
#   ★ 本仓内任何判据都看不见那个分歧。
# ⇒ 装配与部署命令见 web/README.md。
web/assets/
